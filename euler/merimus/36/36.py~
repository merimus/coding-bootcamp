
def twobasepalindrome(n):
    # palindrome in base 10?
    a = list(str(n))
    b = list(a)
    b.reverse()
    print a, b
    print cmp(a, b)

    # palindrome in base 2
    a = list(bin(n)[2:])
    b = list(a)
    b.reverse()
    print a, b
    print cmp(a, b)
