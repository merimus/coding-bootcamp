
def test(a, b):
    n = a / float(b)
    sa = str(a)
    sb = str(b)
    if sa[0] == sb[0]:
        x = int(sa[1])
        y = int(sb[1])
        if n == x / float(y):
            print a, b
    if sa[0] == sb[1]:
        x = int(sa[1])
        y = int(sb[0])
        if n == x / float(y):
            print a, b
    if sa[1] == sb[0]:
        x = int(sa[0])
        y = int(sb[1])
        if n == x / float(y):
            print a, b
    if sa[1] == sb[1]:
        x = int(sa[0])
        y = int(sb[0])
        if n == x / float(y):
            print a, b
